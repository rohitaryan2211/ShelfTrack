{
  "name": "scripts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "seed": "tsx ingest/seed.ts",
    "seed:publish": "tsx ingest/seed.ts --publish",
    "seed:dry": "tsx ingest/seed.ts --dry-run",
    "update": "tsx ingest/update.ts",
    "update:dry": "tsx ingest/update.ts --dry-run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.50.0",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "tsx": "^4.19.4",
    "@types/node": "^20.0.0"
  }
}
